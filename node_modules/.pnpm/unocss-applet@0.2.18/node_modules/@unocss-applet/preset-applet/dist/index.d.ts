import { Preset } from 'unocss';
import { PresetMiniOptions, Theme } from '@unocss/preset-mini';
export { Theme } from '@unocss/preset-mini';

interface PresetAppletOptions extends PresetMiniOptions {
    /**
     * Enable applet, only build applet should be true
     * e.g. In uniapp set `enable: !(process.env.UNI_PLATFORM === 'h5')` to disable for h5
     * @default true
     */
    enable?: boolean;
}
declare function presetApplet(options?: PresetAppletOptions): Preset<Theme>;

export { PresetAppletOptions, presetApplet as default };
