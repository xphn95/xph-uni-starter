'use strict';

const transformerApplet = require('@unocss-applet/transformer-applet');



for (const k in transformerApplet) {
	if (k !== 'default' && !exports.hasOwnProperty(k)) exports[k] = transformerApplet[k];
}
