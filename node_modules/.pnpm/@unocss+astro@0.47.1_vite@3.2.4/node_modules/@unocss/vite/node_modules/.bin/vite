#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -z "$NODE_PATH" ]; then
  export NODE_PATH="/mnt/c/Users/ning/Desktop/uni-uno-demo2/xph-uni-starter/node_modules/.pnpm/node_modules"
else
  export NODE_PATH="$NODE_PATH:/mnt/c/Users/ning/Desktop/uni-uno-demo2/xph-uni-starter/node_modules/.pnpm/node_modules"
fi
if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../../../../../vite@3.2.4_@types+node@18.11.10/node_modules/vite/bin/vite.js" "$@"
else
  exec node  "$basedir/../../../../../../vite@3.2.4_@types+node@18.11.10/node_modules/vite/bin/vite.js" "$@"
fi
