'use strict';

const transformerAttributify = require('@unocss-applet/transformer-attributify');



for (const k in transformerAttributify) {
	if (k !== 'default' && !exports.hasOwnProperty(k)) exports[k] = transformerAttributify[k];
}
